This README file contains information on the contents of the meta-pcbd-ast layer.

Build process:
$ git clone https://github.com/openbmc/openbmc.git
$ cd openbmc
$ git clone https://gitea.pcbdesign.hu/bkardos/meta-pcbd-ast.git -b dev-honister
$ . setup congax7-ast2500
$ bitbake obmc-phosphor-image

$ cd tmp/deploy/images/congax7-ast2500
$ sudo cp -L image-kernel /srv/tftp/
$ sudo cp -L flash-congax7-ast2500 /srv/tftp/

